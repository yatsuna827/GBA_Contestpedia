{
  "name": "gba_contestpedia",
  "type": "module",
  "scripts": {
    "preview": "bunx --bun vite preview",
    "build": "bunx --bun vite build && prettier ./docs/**/*.{html,css} --write --log-level=warn",
    "format:docs": "prettier ./docs/ --write --log-level=warn",
    "format:src": "prettier ./src/ --write --log-level=warn"
  },
  "dependencies": {
    "hono": "^4.9.9"
  },
  "devDependencies": {
    "@hono/vite-ssg": "^0.2.0",
    "bun-types": "^1.2.23",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.7",
    "vite-plugin-pwa": "^1.0.3"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 120,
    "trailingComma": "es5"
  }
}