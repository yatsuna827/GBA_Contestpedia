{
  "name": "gba_contestpedia",
  "module": "./src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run ./src/index.ts",
    "build-and-format": "bun run build ; bun run format:docs",
    "check": "bun x tsc",
    "format:docs": "bun x prettier ./docs/ --write --log-level=warn",
    "format:src": "bun x prettier ./src/ --write --log-level=warn"
  },
  "devDependencies": {
    "@types/react": "^18.2.34",
    "@types/react-dom": "^18.2.14",
    "bun-types": "^1.0.7",
    "prettier": "^3.0.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 120,
    "trailingComma": "es5"
  }
}